version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        PLATFORM: linux/amd64
    environment:
      ENV: test
    ports:
      - "8080:3000"
    networks:
      - app_network

networks:
  app_network:
    external: true
